前端工程师（15k 月薪）面试题集
该薪资档位面试核心考察基础扎实度 + 框架实战能力 + 工程化落地经验，题目覆盖高频考点，兼顾原理与实操，适合 2-3 年经验前端开发者。
一、JavaScript 基础（必问，占比 30%）
请解释 var、let、const 的区别，包括作用域、提升、重复声明、修改等方面。
什么是原型链？**proto** 和 prototype 的关系是什么？如何实现继承（至少两种方式）？
请说明浏览器事件循环中宏任务和微任务的执行顺序，列举 3 种常见宏任务和 3 种常见微任务，并分析以下代码的输出结果：
javascript

```javascript
运行;
console.log("A");
setTimeout(() => console.log("B"), 0);
Promise.resolve().then(() => console.log("C"));
new Promise((resolve) => {
  console.log("D");
  resolve();
}).then(() => console.log("E"));
console.log("F");
```

什么是闭包？闭包的应用场景有哪些？闭包可能带来的问题是什么？
请实现一个防抖函数 debounce 和节流函数 throttle，并说明两者的区别和适用场景。
二、React 核心（技术栈重点，占比 30%）
请说明 useState 的更新机制，为什么直接修改状态不会触发组件重渲染？函数式更新（setState(prev => prev + 1)）的适用场景是什么？
useEffect 的依赖数组作用是什么？依赖数组为空、省略、传参三种情况的区别是什么？如何避免 useEffect 的无限循环？
什么是 React 的虚拟 DOM？虚拟 DOM 的优势是什么？Diff 算法的核心策略（同层比较、key 的作用）是什么？
React 组件通信有哪些方式？请分别说明适用场景（父子、兄弟、跨层级）。
请设计一个自定义 Hook useLocalStorage，实现将状态同步到 localStorage，要求支持初始值、自动序列化 / 反序列化。
三、工程化与工具链（实战能力，占比 20%）
Webpack 的核心概念有哪些（entry、output、loader、plugin）？loader 和 plugin 的区别是什么？
什么是 Tree-Shaking？如何确保 Tree-Shaking 生效？
前端项目中如何实现代码分割？列举至少两种方式（如路由懒加载、动态导入）。
开发环境和生产环境的 Webpack 配置有哪些区别？
前端项目如何接入 ESLint 和 Prettier？两者的区别是什么？如何解决两者的规则冲突？
四、浏览器与网络（基础必备，占比 10%）
浏览器的缓存机制分为哪几种？强缓存和协商缓存的区别是什么？304 状态码的触发条件是什么？
请说明从输入 URL 到页面渲染的完整流程。
什么是跨域？跨域的解决方案有哪些（至少 3 种）？
如何优化前端页面的首屏加载速度？列举至少 5 种方案。
五、项目实战与综合题（考察解决问题能力，占比 10%）
你在项目中遇到过哪些性能问题？如何定位和解决的？
React 项目中常见的内存泄漏场景有哪些？如何避免？
如何实现一个前端权限管理系统？（包括路由权限、按钮权限）
手写题：实现一个简易的 Promise，至少支持 then 方法和链式调用。
答案要点提示（核心得分点）
JavaScript 基础第 3 题输出结果
A → D → F → C → E → B
解析：同步代码优先执行 → Promise 构造函数内代码同步执行 → 微任务（Promise.then）优先于宏任务（setTimeout）执行 → 微任务按入队顺序执行。
React 自定义 Hook 第 5 题参考实现

```javascript
运行;
function useLocalStorage(key, initialValue) {
  const [value, setValue] = useState(() => {
    // 初始化时从 localStorage 读取
    const storedValue = localStorage.getItem(key);
    return storedValue ? JSON.parse(storedValue) : initialValue;
  });

  useEffect(() => {
    // 状态更新时同步到 localStorage
    localStorage.setItem(key, JSON.stringify(value));
  }, [key, value]);

  return [value, setValue];
}
防抖函数参考实现;
```

运行

```javascript
function debounce(fn, delay) {
  let timer = null;
  return function (...args) {
    if (timer) clearTimeout(timer);
    timer = setTimeout(() => {
      fn.apply(this, args);
    }, delay);
  };
}
```

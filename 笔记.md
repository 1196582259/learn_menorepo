1. key in obj 会检查对象及其原型链上是否有该属性，包括继承的属性。
2. hasOwnProperty 方法只会检查对象自身是否有该属性，不包括继承的属性。Object.keys只能获取对象自身的可枚举属性键。
3. Object.prototype.toString 是最精准的类型检测工具。
4. Object.getOwnPropertySymbols 方法可以获取对象所有的 Symbol 类型的属性键。
5. Reflect.ownKeys 方法可以获取对象所有的属性键，包括 Symbol 类型的属性键和 不可枚举属性键。
